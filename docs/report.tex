\documentclass[a4paper,11pt]{article}

% Page geometry
\usepackage[margin=2cm,top=2.5cm,bottom=2.5cm]{geometry}

% Essential packages
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{microtype}

% COLORS
\usepackage[dvipsnames]{xcolor}
\definecolor{background}{HTML}{ffffff}
\definecolor{surface}{HTML}{ffffff}
\definecolor{textmuted}{HTML}{7a7974}
\definecolor{accent}{HTML}{7b2020}
\definecolor{grid}{HTML}{dcd9d5}
\definecolor{success}{HTML}{437a22}
\definecolor{deepred}{HTML}{5c1010}

% Modern font
\usepackage[scaled]{helvet}
\renewcommand{\familydefault}{\sfdefault}
\usepackage{libertinus}

% Tables
\usepackage{booktabs}
\usepackage{siunitx}

% Float placement control
\usepackage{float}

% Bibliography
\usepackage[style=numeric,autocite=superscript,backend=biber,sorting=none]{biblatex}
\addbibresource{./report.bib}

% References
\makeatletter
\newcommand*{\sref}[1]{%
  \hyperref[{#1}]{%
    \color{accent}\textsuperscript{\ref*{#1}}%
  }%
}
\makeatother

% Graphics and links
\usepackage{graphicx}
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=accent,
  urlcolor=accent,
  citecolor=accent,
  pdfborder={0 0 0}
}

% Headers/footers
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small Tableau Data Visualization Report}
\fancyhead[R]{\thepage}
\fancyfoot[C]{\small Itziar Morales Rodr\'{i}guez}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\color{grid}\hrule height 0.4pt}

% Title formatting
\renewcommand{\familydefault}{\sfdefault}
\usepackage{titlesec}
\titleformat{\section}{\large\bfseries\color{accent}\sffamily}{\thesection}{0.5em}{}
\titleformat{\subsection}{\normalsize\bfseries\color{black}\sffamily}{\thesubsection}{0.5em}{}
\titlespacing*{\section}{0pt}{1.5em}{0.8em}
\titlespacing*{\subsection}{0pt}{1.2em}{0.6em}

% Enumerations
\usepackage{enumitem}
\setlist[itemize]{leftmargin=*}
\setlist[enumerate]{leftmargin=*}

% Background
\usepackage{afterpage}
\afterpage{\pagecolor{background}\color{black}}

\title{\color{accent}\Huge Tableau Data Visualization\\Assignment Report}
\author{\Large\color{black}Itziar Morales Rodr\'{i}guez}
\date{\large\color{black}\today}

\begin{document}
\pagecolor{background}\color{black}

\maketitle
\vspace{1em}
\hrule height 1pt \color{black}

\tableofcontents
\newpage

\section{Introduction}

\par
This project explores the relationship between state firearm legislation and
gun violence in the United States using data spanning 1976--2023. While
estimated household gun ownership is included as a contextual variable, it is
not treated as the primary explanatory factor. Evidence from countries such as
Finland and Switzerland where high gun ownership coexists with low rates of gun
violence, suggests that the regulatory environment surrounding firearms may
matter more than ownership prevalence alone. This project therefore focuses on
whether the strength and type of state firearm laws is associated with lower
rates of firearm homicide, and whether changes in legislation over time
correspond to measurable shifts in a state's gun violence standing.

\subsection{Methodology}

\par
The project followed the process below to achieve the goal described above:
\begin{enumerate}
	\item Dataset selection and join strategy design
	\item Data analysis outside Tableau
	\item Research questions derived from data exploration
	\item Visualization types needed to answer each question and storytell
	\item Data source connection, relationships, and calculated fields in Tableau
	\item Tableau feature selection (parameters, sets, filters, actions, tooltips)
	\item Dashboard design and interactive storytelling
	\item Use of AI assistance for laborious or repetitive tasks
	      (see Section~\ref{sec:ai})
\end{enumerate}

\subsection{Sources}

\par
Numerous sources are used throughout the project such as:

\begin{itemize}
	\item Official dataset documentation and
	      codebooks~\autocite{ownershipproxy,statefirearmlaw}
	\item Harvard Dataverse and Tufts CTSI dataset
	      metadata~\autocite{kang2023,statefirearmlaw}
	\item Academic literature on firearm ownership
	      proxies~\autocite{extendingfss,kang2023}
	\item Academic literature on gun law
	      effects~\autocite{kopernij1999,koper2004,donohue2022}
	\item Tableau official documentation~\autocite{tableau}
	\item Lecture notebooks and course materials
	\item AI assistance (declared in Section~\ref{sec:ai})
\end{itemize}

\par
Regardless of the source, these are indicated through bibliography and
references, or in the case of AI, an explicit note indicating the model and
prompt used.

\subsection{AI Usage Declaration}\label{sec:ai}

\par
AI assistance was used for laborious, repetitive, or mechanical tasks
throughout this project. All AI output was reviewed critically before use.
Analytical conclusions, Tableau visualizations, and interpretation of results
were not produced by AI. The model used in all cases was
\textbf{Claude Sonnet 4.6}, accessed via the Perplexity AI assistant.
Detailed prompts and responses are documented inline at each point of use;
the table below provides a summary index.

\begin{table}[H]
	\centering
	\begin{tabular}{lll}
		\toprule
		\textbf{Task} & \textbf{Model} & \textbf{Section} \\
		\midrule
		Dataset research and pairing strategy  & Claude Sonnet 4.6 & \ref{sec:datasets} \\
		State-abbreviation calculated field    & Claude Sonnet 4.6 & \ref{sec:viz:q1} \\
		Document labelling and cross-referencing & Claude Sonnet 4.6 & \ref{sec:ai:labels} \\
		Report structuring and LaTeX formatting & Claude Sonnet 4.6 & \ref{sec:ai:latex} \\
		Supplementary chart generation          & Claude Sonnet 4.6 & \ref{sec:viz:q3} \\
		\bottomrule
	\end{tabular}
	\caption{AI usage index}
	\label{tab:ai}
\end{table}

\subsubsection{Document Labelling and Cross-Referencing}\label{sec:ai:labels}

\par
Adding \texttt{\textbackslash label} tags to named sections and propagating
\texttt{\textbackslash ref} calls consistently throughout a long document is
mechanical and error-prone when done manually. Modern LLMs are well-suited to
this kind of structural text analysis, as they can parse the full document,
identify every mention of a concept, and apply uniform markup in a single pass.

\begin{quote}
\textbf{Prompt:} \textit{Add labels to questions Q1, Q2 and Q3 in the document
and reference those labels whenever those questions are referenced in the
document.}
\end{quote}

\begin{quote}
\textbf{Response summary:} Added \texttt{\textbackslash label\{q1\}},
\texttt{\textbackslash label\{q2\}}, \texttt{\textbackslash label\{q3\}} to
the three \texttt{\textbackslash subsubsection} headings, inserted
\texttt{\textbackslash sref\{q1/q2/q3\}} at the end of each item in the
numbered question list, and prefixed the corresponding Conclusions bullets and
Visualization Summary table rows with the matching \texttt{\textbackslash sref}
call, creating clickable hyperlinks throughout the PDF.
\end{quote}

\subsubsection{Report Structuring and LaTeX Formatting}\label{sec:ai:latex}

\par
AI was used to assist with LaTeX boilerplate, section organisation, consistent
page-break placement, and bibliography management --- tasks that are
time-consuming but do not require analytical judgement.

\begin{quote}
\textbf{Prompt:} \textit{For visualizations, make it so each of them is on a
different page alongside with their text section, so page breaks are consistent
and it doesn't break in weird places.}
\end{quote}

\begin{quote}
\textbf{Response summary:} Added \texttt{\textbackslash usepackage\{float\}}
to the preamble, inserted \texttt{\textbackslash newpage} before each
visualization subsection (Q1, Q2, Q3, Dashboards, Summary), and changed all
figure placement specifiers from \texttt{[h]} to \texttt{[H]} to force
figures to appear exactly at their source position rather than floating.
\end{quote}

\section{Datasets}\label{sec:datasets}

\par
Two public datasets were selected and used in conjunction for this project.
Both are accessible through their corresponding links and are available in
different formats. The first dataset was identified independently; finding a
compatible second dataset was AI-assisted (Claude Sonnet 4.6;
see Section~\ref{sec:ai}).

\begin{quote}
\textbf{Prompt:} \textit{I already have a state-year level dataset on
estimated household gun ownership in the US. I need a second dataset that
contains information about firearm legislation on a state-year basis so I
can join them easily. What publicly available datasets would you recommend?}
\end{quote}

\begin{quote}
\textbf{Response summary:} Recommended the Tufts CTSI State Firearm Law
Database, noting that it tracks 72 binary law provisions per U.S.\ state
per year from 1976 to 2024, shares the same state--year grain as the
ownership proxy dataset, and is widely used in public health research,
making it a straightforward join candidate.
\end{quote}

\subsection{Dataset 1: Firearm Suicide Proxy for Household Gun Ownership, 1949--2023}

\par
The first dataset\autocite{ownershipproxy} is a state-year level proxy for
household gun ownership in the United States. Direct gun ownership data is
not publicly available at the state level, so this dataset uses the firearm
suicide proxy methodology: the proportion of suicides committed with a firearm
(FSS) is used as an indirect estimate of household gun prevalence by state and
year. It is hosted on Harvard Dataverse.

\begin{itemize}
	\item \textbf{Source}: Harvard Dataverse, Kang et al.\autocite{kang2023}
	\item \textbf{Grain}: One row per U.S. state per year
	\item \textbf{Key columns}: State, Year, FSS (firearm suicide share),
	      firearm suicides, total suicides, firearm homicide rate,
	      total homicides, population
	\item \textbf{Date span}: 1949--2023 (75 years, 50 states + D.C.)
	\item \textbf{Format}: Comma-separated values (\texttt{.csv})
\end{itemize}

\par
This dataset was chosen because it provides a long term, population adjusted,
and academically validated measure of gun prevalence that can be compared
across states and over time. FSS is not a literal count of guns per household
but an indirect proxy widely used in public health and policy
research\autocite{extendingfss}. FSS is retained in this project as a
contextual indicator of estimated gun prevalence on the scatter plot;
firearm homicide rate serves as the primary outcome measure throughout.

\subsection{Dataset 2: State Firearm Law Database, 1976--2024}

\par
The second dataset\autocite{statefirearmlaw} is maintained by the State
Firearm Law Research Group at Tufts University and tracks the presence or
absence of 72 firearm safety law provisions across all 50 U.S. states for
every year from 1976 to 2024.

\begin{itemize}
	\item \textbf{Source}: Tufts CTSI --- State Firearm Law
	      Database\autocite{statefirearmlaw}
	\item \textbf{Grain}: One row per U.S. state per year
	\item \textbf{Key columns}: State, Year, 72 binary law indicator columns
	      (e.g.\ permit required, background check, waiting period,
	      stand-your-ground, child access prevention)
	\item \textbf{Date span}: 1976--2024
	\item \textbf{Format}: Excel (\texttt{.xlsx})
\end{itemize}

\par
This dataset was chosen because it provides a historically accurate,
year-by-year record of state firearm legislation, enabling analysis of
whether law changes correspond to changes in gun violence rates. It is
non partisan by design and is widely used in academic public health
research\autocite{statefirearmlaw}.

\subsection{Join Strategy}

\par
Both datasets were connected in Tableau using a \textbf{relationship} on the
shared keys \texttt{state} and \texttt{year}, configured with
\textbf{many-to-many} cardinality and \textbf{some records match} referential
integrity on both sides. Many-to-many cardinality is appropriate here because
both tables share the same grain (one row per state per year) and neither side
acts as a unique dimension table. The ``some records match'' setting instructs
Tableau not to assume all keys are present in both tables, handling
non-overlapping rows safely without discarding data prematurely. Tableau
resolves the relationship by performing aggregations on each table
independently before combining results, preventing row duplication. The
overlapping date range of both datasets yields an effective analysis window
of \textbf{1976--2023}.

\subsection{Analysis}

\par
Prior to connecting the data in Tableau, both files were inspected to
understand structure, null patterns, and label consistency.
Key observations:

\begin{itemize}
	\item Both datasets use identical full state name format
	      (e.g.\ \texttt{Alabama}, \texttt{North Carolina}),
	      allowing for a join on state
	\item The ownership proxy file includes several pre calculated rate
	      columns derived from raw counts and population
	\item The law database contains 72 binary columns but it also includes
	      a total law count column that can be used
	\item Both datasets include a sufficient overlapping year window between
	      1976--2023 that can be used to join
\end{itemize}

\subsection{Processing}

\par
Both source files were connected to Tableau without modification to the raw
data. No column renaming, deduplication, or external pre-processing was
required. The only structural adjustment was the exclusion of the District of
Columbia via a Tableau filter, as it is present in the ownership proxy dataset
but absent from the state firearm laws dataset, which would have prevented a
valid join for that entity.

\subsection{Research Questions}

\par
Three questions were formulated based on data exploration and storytelling intent:

\begin{enumerate}
	\item Is a state's firearm homicide rate associated with the number
	      and type of firearm laws it has in place?\sref{q1}
	\item How have firearm homicide rates evolved over time depending on
	      the firearm laws passed in each state, whether measured by
	      the quantity of laws or by the categories of law types enacted?\sref{q2}
	\item For a specific state, does passing more firearm laws correspond
	      to a reduction in firearm homicide rates, and how does this
	      affect its ranking relative to other states?\sref{q3}
\end{enumerate}

\subsubsection{Question 1: Homicide Rates and the Law Landscape}\label{q1}

\par
This is the entry point of the analysis. Before drawing any temporal or causal
conclusions, it is necessary to establish whether a cross-sectional pattern
exists: do states that have enacted more laws or certain types of laws, tend to
have lower firearm homicide rates? The question is meant to ask whether the law
and violence landscape are associated when viewed together. FSS is included as
a contextual variable to show where estimated gun ownership sits in this
picture, without treating it as an outcome or a direct cause.

\subsubsection{Question 2: Homicide Trends by Law Profile Over Time}\label{q2}

\par
If a cross-sectional association exists, the natural follow up is whether it
has strengthened or weakened over time. This question was chosen because the
datasets span nearly five decades, allowing for more temporal depth. By
grouping states according to their law profile (types of laws or number of laws
passed), it becomes possible to observe whether the trajectories have diverged,
and whether that divergence accelerated after key legislative periods such as
the early 1990s.

\subsubsection{Question 3: State-Level Law History and Homicide Standing}\label{q3}

\par
The first two questions analyze the broader picture whilst this third question
brings the analysis down to the individual state, making it personally relevant
and explorable for any viewer. The motivation is to let someone examine their
own state: when did it pass laws, how many, and does its current homicide
ranking reflect that legislative history? It is an exploration due to the
nature of each state being individualized, making it harder to draw causality
unlike with the broader analysis.

\section{Visualization Prep}

\par
Before building charts in Tableau, design and structural decisions were made
to align visualizations with the storytelling intent and assignment
requirements.

\subsection{Storytelling}

\par
The project follows a four-act narrative structure. Act 1 opens with the
geographic baseline: where gun violence is highest and what the law landscape
looks like across states. Act 2 introduces the temporal dimension: how
homicide rates have diverged between state groups defined by their law
profile over time. Act 3 connects both dimensions in the ownership-laws-violence
scatter. Act 4 closes with the state-level exploration: does a specific
state's legislative history correspond to an improvement in its standing?
The viewer moves from observation to exploration to interpretation.

\subsection{Colors and Themes}\label{colors}

\par
Colors were chosen to encode meaning consistently:

\begin{itemize}
	\item A \textbf{sequential red palette} encodes gun violence rates on
	      maps and scatter plots, where darker shades indicate higher rates.
	\item A \textbf{diverging palette} separates high-law from low-law states
	      in trend and comparison views.
	\item \textbf{Set-based color} encodes user-selected states (highlighted)
	      vs.\ unselected states (grey) in the interactive bar chart.
	\item Backgrounds are white and gridlines are minimal to reduce visual
	      noise and keep focus on data marks.
\end{itemize}

\subsection{Tableau Feature Choices}

\par
The following Tableau features were planned and used across this project:

\begin{itemize}
	\item \textbf{Calculated fields}: Composite law count score (sum of
	      72 binary columns), law category classification, above/below
	      national average flag, estimated gun-owning households
	\item \textbf{Parameters}: Year selector, Top N selector for
	      ranked bar chart, State selector, Law-categorization toggle
	      (law quantity tier vs.\ law type group)
	\item \textbf{Sets and Combined Sets}: Selected states set for
	      interactive bar chart, top/bottom N sets combined for
	      ranked bar chart
	\item \textbf{Filters}: State multiselect, year range
	\item \textbf{Dashboard actions}: Set action (click state on map to
	      highlight it in the bar chart and update the state line chart)
	\item \textbf{Reference lines}: National average in trend views
	\item \textbf{Trend lines}: Linear trend in scatter plot
	      (from Analytics pane)
\end{itemize}

% -----------------------------------------------------------------------
\section{Visualizations}
% -----------------------------------------------------------------------

\par
A consistent set of calculated fields and a shared color scheme were applied
across all visualizations. Field names are self-explanatory (e.g.\
\texttt{CF\_law\_count}, \texttt{CF\_show\_state}, \texttt{P\_year})
to ensure readability and reproducibility.

% --- Q1 -----------------------------------------------------------------
\newpage
\subsection{Q1 -- Homicide Rates and the Law Landscape}\label{sec:viz:q1}

\par
Two charts answer Question 1\sref{q1}. The first is a \textbf{choropleth map}
displaying the firearm homicide rate across all U.S.\ states for a
user-selected year, encoded with a sequential red palette so darker states
indicate higher rates. The second is a \textbf{scatter plot} where each
point represents one state: the X axis shows estimated gun ownership (FSS)
as contextual background, the Y axis shows the composite law count, and
color encodes the firearm homicide rate. A year parameter and a
law-categorization parameter allow the viewer to scrub through time and
switch the color encoding between law tier and law type group, enabling
both a quantitative and a qualitative reading of the law landscape.

\par
The choropleth map uses state abbreviations as mark labels. Generating the
calculated field that maps all 50 full state names to their two-letter
abbreviations was AI-assisted (Claude Sonnet 4.6; see Section~\ref{sec:ai}),
as writing 50 individual \texttt{WHEN} clauses by hand is purely mechanical
and error-prone.

\begin{quote}
\textbf{Prompt:} \textit{Generate a Tableau calculated field using a CASE
statement that maps each U.S.\ state's full name to its two-letter
abbreviation for all 50 states. Only output the code.}
\end{quote}

\begin{quote}
\textbf{Response:}
\begin{verbatim}
CASE [State]
  WHEN "Alabama" THEN "AL"
  WHEN "Alaska" THEN "AK"
  WHEN "Arizona" THEN "AZ"
  WHEN "Arkansas" THEN "AR"
  WHEN "California" THEN "CA"
  WHEN "Colorado" THEN "CO"
  WHEN "Connecticut" THEN "CT"
  WHEN "Delaware" THEN "DE"
  WHEN "Florida" THEN "FL"
  WHEN "Georgia" THEN "GA"
  WHEN "Hawaii" THEN "HI"
  WHEN "Idaho" THEN "ID"
  WHEN "Illinois" THEN "IL"
  WHEN "Indiana" THEN "IN"
  WHEN "Iowa" THEN "IA"
  WHEN "Kansas" THEN "KS"
  WHEN "Kentucky" THEN "KY"
  WHEN "Louisiana" THEN "LA"
  WHEN "Maine" THEN "ME"
  WHEN "Maryland" THEN "MD"
  WHEN "Massachusetts" THEN "MA"
  WHEN "Michigan" THEN "MI"
  WHEN "Minnesota" THEN "MN"
  WHEN "Mississippi" THEN "MS"
  WHEN "Missouri" THEN "MO"
  WHEN "Montana" THEN "MT"
  WHEN "Nebraska" THEN "NE"
  WHEN "Nevada" THEN "NV"
  WHEN "New Hampshire" THEN "NH"
  WHEN "New Jersey" THEN "NJ"
  WHEN "New Mexico" THEN "NM"
  WHEN "New York" THEN "NY"
  WHEN "North Carolina" THEN "NC"
  WHEN "North Dakota" THEN "ND"
  WHEN "Ohio" THEN "OH"
  WHEN "Oklahoma" THEN "OK"
  WHEN "Oregon" THEN "OR"
  WHEN "Pennsylvania" THEN "PA"
  WHEN "Rhode Island" THEN "RI"
  WHEN "South Carolina" THEN "SC"
  WHEN "South Dakota" THEN "SD"
  WHEN "Tennessee" THEN "TN"
  WHEN "Texas" THEN "TX"
  WHEN "Utah" THEN "UT"
  WHEN "Vermont" THEN "VT"
  WHEN "Virginia" THEN "VA"
  WHEN "Washington" THEN "WA"
  WHEN "West Virginia" THEN "WV"
  WHEN "Wisconsin" THEN "WI"
  WHEN "Wyoming" THEN "WY"
END
\end{verbatim}
\end{quote}

\begin{figure}[H]
	\centering
	\includegraphics[width=\textwidth]{../figures/Q1Chart1.png}
	\caption{Q1 -- Firearm Homicide Rate by State (Choropleth Map)}
	\label{fig:q1chart1}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=\textwidth]{../figures/Q1Chart2.png}
	\caption{Q1 -- Law Count vs.\ Estimated Gun Ownership (FSS), colored by Firearm Homicide Rate (Scatter Plot)}
	\label{fig:q1chart2}
\end{figure}

\textbf{Insights}: States with fewer laws cluster towards higher firearm
homicide rates. The FSS axis provides ownership context but is not
treated as a causal variable; the law count and law type dimensions
are the primary analytical lenses.

% --- Q2 -----------------------------------------------------------------
\newpage
\subsection{Q2 -- Homicide Trends by Law Profile Over Time}

\par
An \textbf{interactive line chart} answers Question 2\sref{q2}. The X axis
shows year (1976--2023), the Y axis shows firearm homicide rate, and each
line represents a group of states defined by the active law-categorization
parameter. When set to ``Law Quantity Tier'', states are grouped into
few-law, average-law, and high-law tertiles based on total law count.
When set to ``Law Type Group'', lines reflect groups defined by the
categories of laws present. This toggle makes it possible to analyse
both the volume and the nature of legislation in a single chart.

\begin{figure}[H]
	\centering
	\includegraphics[width=\textwidth]{../figures/Q2Chart.png}
	\caption{Q2 -- Firearm Homicide Rate Over Time by Law Profile (1976--2023)}
	\label{fig:q2chart}
\end{figure}

\textbf{Insights}: States grouped by higher law counts show a steeper
decline in firearm homicide rates from the 1990s onward. The divergence
between groups widens over time, and the pattern holds across both the
law quantity and law type groupings.

% --- Q3 -----------------------------------------------------------------
\newpage
\subsection{Q3 -- State-Level Law History and Homicide Standing}\label{sec:viz:q3}

\par
Two charts answer Question 3\sref{q3}. The first is a \textbf{line chart}
showing the total number of firearm laws in effect over time for a
user-selected state (driven by the \texttt{P\_State} parameter). This
makes it easy to identify when a state underwent major legislative shifts.
The second is a \textbf{bar chart} ranking all states by firearm homicide
rate in a selected year: bars are colored by law-group category, and the
user-selected state is highlighted so the viewer can immediately see where
it stands relative to all others. Together, the two charts let the viewer
judge whether periods of legislative growth in the selected state coincide
with an improvement in its homicide ranking.

\begin{figure}[H]
	\centering
	\includegraphics[width=\textwidth]{../figures/Q3Chart1.png}
	\caption{Q3 -- Firearm Laws in Effect Over Time for Selected State (Line Chart)}
	\label{fig:q3chart1}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=\textwidth]{../figures/Q3Chart2.png}
	\caption{Q3 -- Firearm Homicide Rate by State for Selected Year, colored by Law Group, Selected State Highlighted (Bar Chart)}
	\label{fig:q3chart2}
\end{figure}

\textbf{Insights}: In several states, sustained increases in law count
coincide with a visible improvement in homicide ranking relative to other
states. The relationship is not uniform and no causal interpretation is
claimed; the viewer is invited to explore individual state trajectories
interactively.

% --- Dashboards ---------------------------------------------------------
\newpage
\subsection{Dashboards}

\par
Two dashboards integrate the individual charts into a cohesive interactive
experience.

\par
\textbf{Dashboard 1} combines the choropleth map (Q1Chart1), the scatter
plot (Q1Chart2), and the law profile line chart (Q2Chart). A click action
on the map filters the scatter to highlight the selected state; the law
profile line chart provides immediate temporal context. The shared
law-categorization parameter applies across all three views, meaning a
single toggle switches the analytical lens for the entire dashboard.
This gives the viewer a complete overview of the geographic, cross-sectional,
and trend dimensions simultaneously.

\begin{figure}[H]
	\centering
	\includegraphics[width=\textwidth]{../figures/Dashboard1.png}
	\caption{Dashboard 1 -- Geographic Overview, Law--Homicide Scatter, and Law Profile Trends}
	\label{fig:db1}
\end{figure}

\par
\textbf{Dashboard 2} combines the choropleth map (Q1Chart1), the comparative
bar chart (Q3Chart2), and the single-state law history line chart
(Q3Chart1). A shared state parameter drives both the bar chart highlight
and the line chart, letting the viewer select any state and instantly
see where it stands relative to all others and how its legislative
history has unfolded over time.

\begin{figure}[H]
	\centering
	\includegraphics[width=\textwidth]{../figures/Dashboard2.png}
	\caption{Dashboard 2 -- Geographic Map, State Homicide Ranking Bar Chart, and Selected State Law History}
	\label{fig:db2}
\end{figure}

% --- Summary table ------------------------------------------------------
\newpage
\subsection{Visualization Summary}

\begin{table}[H]
	\centering
	\begin{tabular}{rcll}
		\toprule
		\textbf{Question} & \textbf{Chart}          & \textbf{Chart Type}      & \textbf{Tableau Feature}             \\
		\midrule
		\midrule
		Q1\sref{q1}       & Fig.~\ref{fig:q1chart1} & Choropleth map           & Calc.\ field, color encoding, param. \\
		\cmidrule{1-4}
		Q1\sref{q1}       & Fig.~\ref{fig:q1chart2} & Scatter plot             & Parameter, calc.\ field, trend line  \\
		\cmidrule{1-4}
		Q2\sref{q2}       & Fig.~\ref{fig:q2chart}  & Line chart (interactive) & Set, set action, reference line      \\
		\cmidrule{1-4}
		Q3\sref{q3}       & Fig.~\ref{fig:q3chart1} & Line chart (per state)   & Parameter, calc.\ field, filter      \\
		\cmidrule{1-4}
		Q3\sref{q3}       & Fig.~\ref{fig:q3chart2} & Bar chart (comparative)  & Calc.\ field, highlight, filter      \\
		\bottomrule
	\end{tabular}
	\caption{Visualization summary table}
	\label{tab:summary}
\end{table}

\par
Table~\ref{tab:summary} summarizes:

\begin{itemize}
	\item 4 chart types used across the project: choropleth map, scatter plot,
	      line chart, and bar chart.
	\item 7 Tableau features used: calculated fields, parameters, sets,
	      combined sets, dashboard set actions, reference lines,
	      and trend lines.
	\item 2 data sources joined by state and year in Tableau,
	      in two different file formats (\texttt{.csv} and \texttt{.xlsx}).
\end{itemize}

\section{Conclusions}

\par
Visualizations reveal the following:

\begin{itemize}
	\item Q1\sref{q1}: States with fewer firearm laws consistently show higher
	      firearm homicide rates, both geographically and in the cross-sectional
	      scatter view [Fig.~\ref{fig:q1chart2}].
	\item Q2\sref{q2}: States with more laws, whether measured by quantity or
	      type category, have seen steeper declines in firearm homicide since
	      the 1990s [Fig.~\ref{fig:q2chart}].
	\item Q3\sref{q3}: In several states, sustained legislative growth coincides
	      with a visible improvement in homicide ranking relative to other
	      states, though no causal claim is made
	      [Fig.~\ref{fig:q3chart1}, Fig.~\ref{fig:q3chart2}].
	\item Gun ownership (FSS) provides useful contextual framing but is
	      not treated as a causal variable; the regulatory environment
	      is the primary analytical lens throughout the project.
\end{itemize}

\par
Limitations include the proxy nature of the FSS ownership estimate,
the absence of statistical controls for confounding variables such as
poverty and urbanization, and the use of a static join on state name
which may mask within-state variation.

\newpage

\section{References}
\printbibliography[heading=none]

\end{document}
