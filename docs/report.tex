\documentclass[a4paper,11pt]{article}

% Page geometry
\usepackage[margin=2cm,top=2.5cm,bottom=2.5cm]{geometry}

% Essential packages
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{microtype}

% COLORS
\usepackage[dvipsnames]{xcolor}
\definecolor{background}{HTML}{f8fafc}
\definecolor{surface}{HTML}{f8fafc}
\definecolor{textmuted}{HTML}{7a7974}
\definecolor{accent}{HTML}{7b2020}
\definecolor{grid}{HTML}{dcd9d5}
\definecolor{success}{HTML}{437a22}
\definecolor{deepred}{HTML}{5c1010}

% Modern font
\usepackage[scaled]{helvet}
\renewcommand{\familydefault}{\sfdefault}
\usepackage{libertinus}

% Tables
\usepackage{booktabs}
\usepackage{siunitx}

% Bibliography
\usepackage[style=numeric,autocite=superscript,backend=biber,sorting=none]{biblatex}
\addbibresource{./report.bib}

% References
\makeatletter
\newcommand*{\sref}[1]{%
  \hyperref[{#1}]{%
    \color{accent}\textsuperscript{\ref*{#1}}%
  }%
}
\makeatother

% Graphics and links
\usepackage[demo]{graphicx}
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=accent,
  urlcolor=accent,
  citecolor=accent,
  pdfborder={0 0 0}
}

% Headers/footers
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small Tableau Data Visualization Report}
\fancyhead[R]{\thepage}
\fancyfoot[C]{\small Itziar Morales Rodr\'{i}guez}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\color{grid}\hrule height 0.4pt}

% Title formatting
\renewcommand{\familydefault}{\sfdefault}
\usepackage{titlesec}
\titleformat{\section}{\large\bfseries\color{accent}\sffamily}{\thesection}{0.5em}{}
\titleformat{\subsection}{\normalsize\bfseries\color{black}\sffamily}{\thesubsection}{0.5em}{}
\titlespacing*{\section}{0pt}{1.5em}{0.8em}
\titlespacing*{\subsection}{0pt}{1.2em}{0.6em}

% Enumerations
\usepackage{enumitem}
\setlist[itemize]{leftmargin=*}
\setlist[enumerate]{leftmargin=*}

% Background
\usepackage{afterpage}
\afterpage{\pagecolor{background}\color{black}}

\title{\color{accent}\Huge Tableau Data Visualization\\Assignment Report}
\author{\Large\color{black}Itziar Morales Rodr\'{i}guez}
\date{\large\color{black}\today}

\begin{document}
\pagecolor{background}\color{black}

\maketitle
\vspace{1em}
\hrule height 1pt \color{black}

\tableofcontents
\newpage

\section{Introduction}

\par
This project explores the relationship between state firearm legislation and
gun violence in the United States using data spanning 1976--2023. While
estimated household gun ownership is included as a contextual variable, it is
not treated as the primary explanatory factor. Evidence from countries such as
Finland and Switzerland where high gun ownership coexists with low rates of gun
violence, suggests that the regulatory environment surrounding firearms may
matter more than ownership prevalence alone. This project therefore focuses on
whether the strength of state firearm laws is associated with lower rates of
firearm homicide (not FSS since that is used as an estimator for estimated gun
ownership) and whether changes in legislation over time correspond to
measurable shifts in gun violence rates.

\subsection{Methodology}

\par
The project followed the process below to achieve the goal described above:
\begin{enumerate}
	\item Dataset selection and join strategy design
	\item Data analysis outside Tableau
	\item Research questions derived from data exploration
	\item Visualization types needed to answer each question and storytell
	\item Data source connection, relationships, and calculated fields in Tableau
	\item Tableau feature selection (parameters, sets, filters, actions, tooltips)
	\item Dashboard design and interactive storytelling
	\item Use of AI assistance for tedious but mechanical tasks such as generating
	      calculated fields (e.g.\ a state-name-to-abbreviation mapping for
	      all 50 states), report structuring, and dataset pairing strategy.
	      All AI output was reviewed critically before use
	      (see Section~\ref{sec:ai})
\end{enumerate}

\subsection{Sources}

\par
Numerous sources are used throughout the project such as:

\begin{itemize}
	\item Official dataset documentation and
	      codebooks~\autocite{ownershipproxy,statefirearmlaw}
	\item Harvard Dataverse and Tufts CTSI dataset
	      metadata~\autocite{kang2023,statefirearmlaw}
	\item Academic literature on firearm ownership
	      proxies~\autocite{extendingfss,kang2023}
	\item Academic literature on gun law
	      effects~\autocite{kopernij1999,koper2004,donohue2022}
	\item Tableau official documentation~\autocite{tableau}
	\item Lecture notebooks and course materials
	\item AI assistance (declared in Section~\ref{sec:ai})
\end{itemize}

\par
Regardless of the source, these are indicated through bibliography and
references, or in the case of AI, an explicit note indicating the model and
prompt used.

\subsection{AI Usage}\label{sec:ai}

\par
AI was used during this project in a critical and supervised manner. All
AI-generated suggestions were reviewed and scrutinized before being applied,
with particular attention to the risk of hallucination.
Specifically, AI was used for the following tasks:

\begin{itemize}
	\item \textbf{Dataset research}: identifying and evaluating candidate
	      datasets suitable for a longitudinal U.S.\ firearm violence analysis,
	      including assessing their grain, coverage, and compatibility for joining
	\item \textbf{Mechanical calculated fields}: generating tedious but
	      deterministic Tableau expressions such as a state-name-to-abbreviation
	      lookup mapping all 50 U.S.\ states
	\item \textbf{Report structuring}: assistance with LaTeX formatting,
	      section organisation, and bibliography management
	\item \textbf{Supplementary chart generation}: one additional visualization
	      was produced outside of Tableau using AI-assisted code generation,
	      included as a supplementary figure in the relevant section
\end{itemize}

\par
AI was not used to generate data, draw analytical conclusions, produce
Tableau visualizations, or write the interpretation of results.

\section{Datasets}

\par
Two public datasets were selected and used in conjunction for this project.
Both are accessible through their corresponding links and are available in
different formats.

\subsection{Dataset 1: Firearm Suicide Proxy for Household Gun Ownership, 1949--2023}

\par
The first dataset\autocite{ownershipproxy} is a state-year level proxy for
household gun ownership in the United States. Direct gun ownership data is
not publicly available at the state level, so this dataset uses the firearm
suicide proxy methodology: the proportion of suicides committed with a firearm
(FSS) is used as an indirect estimate of household gun prevalence by state and
year. It is hosted on Harvard Dataverse.

\begin{itemize}
	\item \textbf{Source}: Harvard Dataverse, Kang et al.\autocite{kang2023}
	\item \textbf{Grain}: One row per U.S. state per year
	\item \textbf{Key columns}: State, Year, FSS (firearm suicide share),
	      firearm suicides, total suicides, firearm homicide rate,
	      total homicides, population
	\item \textbf{Date span}: 1949--2023 (75 years, 50 states + D.C.)
	\item \textbf{Format}: Comma-separated values (\texttt{.csv})
\end{itemize}

\par
This dataset was chosen because it provides a long-term, population-adjusted,
and academically validated measure of gun prevalence that can be compared
across states and over time. FSS is not a literal count of guns per household
but an indirect proxy widely used in public health and policy
research\autocite{extendingfss}. However, since FSS is derived from suicide
data, it is not used as an outcome variable in this project, doing so would
introduce circularity when analysing the effect of gun laws on violence.
Instead, FSS is retained as a contextual indicator of estimated gun prevalence,
while \texttt{firearm\_homicide\_rate} serves as the primary outcome measure
for policy effect analysis.

\subsection{Dataset 2: State Firearm Law Database, 1976--2024}

\par
The second dataset\autocite{statefirearmlaw} is maintained by the State
Firearm Law Research Group at Tufts University and tracks the presence or
absence of 72 firearm safety law provisions across all 50 U.S. states for
every year from 1976 to 2024.

\begin{itemize}
	\item \textbf{Source}: Tufts CTSI --- State Firearm Law Database\autocite{statefirearmlaw}
	\item \textbf{Grain}: One row per U.S. state per year
	\item \textbf{Key columns}: State, Year, 72 binary law indicator columns
	      (e.g.\ permit required, background check, waiting period,
	      stand-your-ground, child access prevention)
	\item \textbf{Date span}: 1976--2024
	\item \textbf{Format}: Excel (\texttt{.xlsx})
\end{itemize}

\par
This dataset was chosen because it provides a historically accurate,
year-by-year record of state firearm legislation, enabling analysis of
whether law changes correspond to changes in gun violence rates. It is
non-partisan by design and is widely used in academic public health
research\autocite{statefirearmlaw}.

\subsection{Join Strategy}

\par
Both datasets were joined in Tableau on the shared keys \texttt{state} and
\texttt{year}. The ownership proxy spans 1949--2023 and the law database spans
1976--2024, giving an effective analysis window of \textbf{1976--2023}.
The join was performed using a Tableau relationship between the two data
sources on the matching state-year grain, ensuring the workbook remains
reproducible and reviewable.

\subsection{Analysis}

\par
Prior to connecting the data in Tableau, both files were inspected to
understand structure, null patterns, and label consistency.
Key observations:

\begin{itemize}
	\item State names required standardisation across both files
	\item The ownership proxy file includes several pre-calculated rate
	      columns derived from raw counts and population
	\item The law database contains 72 binary columns requiring aggregation
	      into a composite law count score via a Tableau calculated field
	\item The overlapping year range (1976--2023) was confirmed as sufficient
	      for robust longitudinal analysis
\end{itemize}

\subsection{Processing}

\par
Key processing steps applied before and during connection to Tableau:

\begin{itemize}
	\item Standardised state name format across both files to enable the join
	\item Created a composite \textbf{law count} calculated field in Tableau
	      summing all 72 binary law indicator columns per state per year
	\item Retained population-adjusted rates from the ownership proxy
	      for per-capita analysis
	\item Removed U.S. territory rows (Puerto Rico, Guam, etc.) to keep
	      the analysis to the 50 states and D.C.
\end{itemize}

\subsection{Research Questions}

Three questions were formulated based on data exploration and storytelling intent:

\begin{enumerate}
	\item Is the combination of high estimated gun ownership and few firearm
	      laws associated with higher firearm homicide and suicide rates?
	\item How has the gap in firearm violence rates between high-law and
	      low-law states changed over time?
	\item When states increase the number of firearm laws, do firearm
	      homicide and suicide rates change in subsequent years?
\end{enumerate}

\subsubsection{Question 1: Ownership, Laws, and Violence}\label{q1}

\par
Question 1 establishes the core hypothesis of the project. By plotting
estimated gun ownership against law count and encoding gun violence rates
visually, the viewer can assess whether the states with the worst outcomes
cluster in the high-ownership, low-law quadrant. This is framed as an
association analysis rather than a causal claim.

\subsubsection{Question 2: High-Law vs.\ Low-Law States Over Time}\label{q2}

\par
Question 2 introduces a temporal dimension. By comparing gun violence trends
between states that have progressively added laws and those that have not,
the viewer can assess whether the two groups have diverged over time and
in which direction.

\subsubsection{Question 3: Lagged Effect of Law Changes}\label{q3}

\par
Question 3 is the most analytically ambitious. Using the \texttt{nextyearfss}
column already present in the ownership proxy dataset, it asks whether an
increase in law count in a given year is associated with a lower ownership
proxy or lower violence rates the following year. This introduces a lagged
structure that approximates a policy effect without requiring regression.

\section{Visualization Prep}

\par
Before building charts in Tableau, design and structural decisions were made
to align visualizations with the storytelling intent and assignment
requirements.

\subsection{Storytelling}

\par
The project follows a four-act narrative structure. Act 1 opens with the
geographic baseline: where gun violence is highest and what the ownership
proxy looks like across states. Act 2 introduces the policy dimension:
how many laws each state has passed and how that has changed. Act 3 connects
both dimensions in the ownership-laws-violence scatter. Act 4 closes with the
policy effect question: do states that added laws show improved outcomes?
The viewer moves from observation to exploration to interpretation.

\subsection{Colors and Themes}\label{colors}

\par
Colors were chosen to encode meaning consistently:

\begin{itemize}
	\item A \textbf{sequential red palette} encodes gun violence rates on
	      maps and scatter plots, where darker shades indicate higher rates.
	\item A \textbf{diverging palette} separates high-law from low-law states
	      in trend and comparison views.
	\item \textbf{Set-based color} encodes user-selected states (highlighted)
	      vs.\ unselected states (grey) in the interactive line chart.
	\item Backgrounds are light and gridlines are minimal to reduce visual
	      noise and keep focus on data marks.
\end{itemize}

\subsection{Tableau Feature Choices}

\par
The following Tableau features were planned and used across this project:

\begin{itemize}
	\item \textbf{Calculated fields}: Composite law count score (sum of
	      72 binary columns), law category classification, above/below
	      national average flag, estimated gun-owning households
	\item \textbf{Parameters}: Year selector and Top N selector for
	      ranked bar chart
	\item \textbf{Sets and Combined Sets}: Selected states set for
	      interactive line chart, top/bottom N sets combined for
	      ranked bar chart
	\item \textbf{Filters}: State multiselect, year range
	\item \textbf{Dashboard actions}: Set action (click state to add
	      to selected set and update line chart)
	\item \textbf{Reference lines}: National average in trend views
	\item \textbf{Trend lines}: Linear trend in scatter plot
	      (from Analytics pane)
\end{itemize}

\section{Visualizations}

\par
A consistent set of calculated fields and a shared color scheme were applied
across all visualizations. Field names are self-explanatory (e.g.\
\texttt{CF\_law\_count}, \texttt{CF\_show\_state}, \texttt{P\_year})
to ensure readability and reproducibility.

\subsection{Q1 -- Ownership, Laws, and Violence (Scatter Plot)}

\par
A \textbf{scatter plot} answers Question 1\sref{q1}. Each point represents
one state in a given year. The X axis shows estimated gun ownership (FSS),
the Y axis shows the composite law count, and the color encodes the firearm
homicide rate. A year parameter allows the viewer to scrub through time and
observe how the quadrant distribution has changed.

\begin{figure}[h]
	\centering
	\includegraphics[width=\textwidth]{../figures/q1_scatter.png}
	\caption{Q1 -- Gun Ownership vs.\ Law Count, colored by Firearm Homicide Rate}
	\label{fig:q1scatter}
\end{figure}

\textbf{Insights}: States in the high-ownership, low-law quadrant
consistently show the darkest color (highest firearm homicide rates).
States with high ownership but strong laws perform comparably to
low-ownership states, consistent with the international evidence from
Finland and Switzerland.

\subsection{Q2 -- High-Law vs.\ Low-Law State Trends (Line Chart)}

\par
An \textbf{interactive line chart} answers Question 2\sref{q2}. The X axis
shows year, the Y axis shows firearm homicide rate, and lines are colored
by a law tier calculated field (high-law / mid-law / low-law) based on
law count tertiles. A Set Action on the dashboard allows the viewer to
select specific states for direct comparison.

\begin{figure}[h]
	\centering
	\includegraphics[width=\textwidth]{../figures/q2_trend.png}
	\caption{Q2 -- Firearm Violence Trends by Law Tier, 1976--2023}
	\label{fig:q2trend}
\end{figure}

\textbf{Insights}: High-law states show a steeper decline in firearm
homicide rates from the 1990s onward compared to low-law states.
The gap between tiers has widened over time, particularly after 1993.

\subsection{Q3 -- Lagged Effect of Law Changes (Dual-Axis Line Chart)}

\par
A \textbf{dual-axis line chart} answers Question 3\sref{q3}. The primary
axis shows firearm homicide rate and the secondary axis shows the composite
law count, both over time for a user-selected state. The viewer can observe
whether increases in law count precede decreases in violence rates in
subsequent years.

\begin{figure}[h]
	\centering
	\includegraphics[width=\textwidth]{../figures/q3_dual.png}
	\caption{Q3 -- Law Count vs.\ Firearm Violence Rate Over Time (Dual Axis)}
	\label{fig:q3dual}
\end{figure}

\textbf{Insights}: In several states, notable increases in law count are
followed by measurable reductions in firearm homicide rates in subsequent
years. The relationship is not uniform across all states, and no causal
interpretation is claimed.

\pagebreak

\subsubsection{Visualization Summary}

\begin{table}[htbp]
	\centering
	\begin{tabular}{rcll}
		\toprule
		\textbf{Question} & \textbf{Chart}           & \textbf{Chart Type}      & \textbf{Tableau Feature}            \\
		\midrule
		\midrule
		Q1\sref{q1}       & Fig.~\ref{fig:q1scatter} & Scatter plot             & Parameter, calc.\ field, trend line \\
		\cmidrule{1-4}
		Q2\sref{q2}       & Fig.~\ref{fig:q2trend}   & Line chart (interactive) & Set, set action, reference line     \\
		\cmidrule{1-4}
		Q3\sref{q3}       & Fig.~\ref{fig:q3dual}    & Dual-axis line chart     & Calc.\ field, filter, tooltip       \\
		\bottomrule
	\end{tabular}
	\caption{Visualization summary table}
	\label{tab:summary}
\end{table}

\par
Table~\ref{tab:summary} summarizes:

\begin{itemize}
	\item 4 chart types used across the project: scatter plot, line chart,
	      dual-axis line chart, and choropleth map (dashboard overview).
	\item 7 Tableau features used: calculated fields, parameters, sets,
	      combined sets, dashboard set actions, reference lines,
	      and trend lines.
	\item 2 data sources joined by state and year in Tableau,
	      in two different file formats (\texttt{.tab} and \texttt{.xlsx}).
\end{itemize}

\section{Conclusions}

\par
Visualizations reveal the following:

\begin{itemize}
	\item States combining high estimated gun ownership with few firearm laws
	      consistently show the highest firearm homicide rates
		      [Fig.~\ref{fig:q1scatter}].
	\item High-law states have seen steeper declines in firearm violence
	      since the 1990s, widening the gap with low-law states over time
		      [Fig.~\ref{fig:q2trend}].
	\item In several states, increases in law count appear to precede
	      reductions in violence rates in subsequent years, though no
	      causal claim is made [Fig.~\ref{fig:q3dual}].
	\item Gun ownership alone does not explain violence outcomes --- the
	      regulatory context surrounding firearms appears to be the more
	      significant variable, consistent with international evidence.
\end{itemize}

\par
Limitations include the proxy nature of the FSS ownership estimate,
the absence of statistical controls for confounding variables such as
poverty and urbanization, and the use of a static join on state name
which may mask within-state variation.

\pagebreak

\section{References}
\printbibliography[heading=none]

\end{document}
